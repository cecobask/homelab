apiVersion: v1
kind: Secret
metadata:
  name: csi-proxmox
  namespace: csi-proxmox
type: Opaque
stringData:
  config.yaml: |
    clusters:
    - url: <PROXMOX_ENDPOINT>
      insecure: false
      token_id: 'kubernetes-csi@pve!csi'
      token_secret: <PROXMOX_TOKEN>
      region: homelab
