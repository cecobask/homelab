apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: cert-manager
  namespace: cert-manager
spec:
  chartRef:
    kind: OCIRepository
    name: cert-manager
  interval: 1h
  values:
    cainjector:
      resources:
        requests:
          cpu: 10m
          memory: 150Mi
    global:
      leaderElection:
        namespace: cert-manager
    prometheus:
      servicemonitor:
        enabled: true
    resources:
      requests:
        cpu: 10m
        memory: 150Mi
    webhook:
      resources:
        requests:
          cpu: 10m
          memory: 80Mi
