apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: prowlarr
spec:
  parentRefs:
  - name: envoy
    namespace: envoy-gateway
  hostnames:
  - prowlarr.cecobask.com
  rules:
  - backendRefs:
    - name: prowlarr
      port: 80
