apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: grafana
  namespace: grafana
spec:
  parentRefs:
  - name: envoy
    namespace: envoy-gateway
  hostnames:
  - grafana.cecobask.com
  rules:
  - backendRefs:
    - name: grafana
      port: 80
